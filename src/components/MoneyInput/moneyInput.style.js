import { makeStyles } from '@material-ui/core/styles';

const moneyInputStyle = makeStyles(() => ({
  root: {
    display: 'flex',
    flexWrap: 'wrap',
    margin: '20px 0',
  },
}));

export default moneyInputStyle;
